package geometry;

/* Standard message to report a twist with covariance.
 */

struct twist_with_covariance_t
{
    // Twist as a twist structure from the geometry package.
    twist_t twist;

    // Covariance matrix.
    double twist_covariance[36];
}
