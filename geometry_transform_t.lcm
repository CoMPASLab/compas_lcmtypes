package geometry;

/* Standard message to represent the transform between two coordinate frames.
 */

struct transform_t
{
    // Translation as a vector3 structure from the geometry package.
    vector3_t translation;

    // Rotation as a quaternion structure from the geometry package.
    quaternion_t rotation;
}
