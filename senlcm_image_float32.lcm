package senlcm;

// Describes an float image

struct image_float32
{
    // microseconds since the epoch
    int64_t  utime;

    int32_t  width;
    int32_t  height;
    int32_t  image_size;
    float     image[image_size];
}