package senlcm;

struct oculus_sonar_t {
    // Microseconds since the epoch
    int64_t utime;

    int32_t nBeams; // width
    int32_t nRanges; // height
    double range;


    // Image
    int32_t image_size;
    byte image[image_size];

    // Bearings
    int16_t bearings[nBeams];
}