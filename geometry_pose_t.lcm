package geometry;

/* Standard message to represent a pose.
 */ 

struct pose_t
{
    // Position as a point structure from the geometry package.
    point_t position;

    // Orientation as a quaternion structure from the geometry package.
    quaternion_t orientation;
}
