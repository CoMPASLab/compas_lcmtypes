package geometry;

/* Standard message to represent a twist.
 */

struct twist_t
{
    // Linear velocity as a vector3 structure from the geometry package.
    vector3_t linear;

    // Angular rate as a vector3 structure from the geometry package.
    vector3_t angular;
}
