package senlcm;

// Describes a depth camera image

struct image_depth
{
    // microseconds since the epoch
    int64_t  utime;

    int32_t  width;
    int32_t  height;
    double range_min;
    double range;
    int32_t  image_size;
    float     image[image_size];
}