package senlcm;

/* Standard message for depth sensors.
 */

struct depth_t
{
    // Standard header.
    standard.header_t header;

    // Depth.
    // SI units = m.
    double depth;

    // Pressure.
    // SI units = mmbar.
    double pressure;
}
